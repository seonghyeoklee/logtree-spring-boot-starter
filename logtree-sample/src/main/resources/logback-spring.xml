<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Appender with Hierarchical Pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%cyan(%d{HH:mm:ss.SSS}) [%highlight(%-5level)] [%yellow(%15.15t)] [%blue(%X{traceId:-NO_TRACE})] %green(%X{visual:-}) %white(%logger{36}.%M) - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- File Appender for JSON format (Loki-friendly) -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.json</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}.json</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.JsonEncoder"/>
    </appender>
    
    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="JSON_FILE"/>
    </root>
    
    <!-- LogTree specific logging -->
    <logger name="io.github.logtree" level="DEBUG"/>
    
    <!-- Spring Framework logging -->
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.web" level="DEBUG"/>
</configuration>